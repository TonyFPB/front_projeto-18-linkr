import styled from "styled-components";

export default function RepostBorder () {
    return (
        <Border></Border>
    )
}

const Border = styled.div`
    width: 611px;
    height: 35px;
    border-radius: 16px;
    background-color: #1E1E1E;
`